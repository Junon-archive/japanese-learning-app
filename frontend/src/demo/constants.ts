/**
 * demo 전용 상수. 체험 흐름의 간격이며 **학습 정책값이 아니다**
 * (`03_UI_UX_SPEC.md`의 `Demo` 절). 이 파일 한 곳에만 두고 테스트는 숫자 대신 이 상수를 참조한다.
 */

/** "몰랐음/애매함"을 고른 표현이 있는 문장을 이만큼 본 문장 뒤에 한 번 더 보여준다. */
export const DEMO_REVIEW_AFTER_SENTENCES = 5

/** 본 문장 수가 이만큼 늘 때마다 probe를 한 번 띄운다. */
export const DEMO_PROBE_EVERY_SENTENCES = 7
