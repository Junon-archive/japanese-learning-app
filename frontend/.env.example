# Vite는 `VITE_` 접두사가 붙은 값을 **번들에 그대로 박아 넣는다.** 빌드 산출물을
# 받은 누구나 읽을 수 있으므로 secret을 여기에 두지 않는다
# (spec/04_SECURITY_AND_DATA.md: "PWA bundle에 secret 금지").
#
# 그래서 이 파일에는 API base URL 하나만 있다. 값을 늘리기 전에 "이것이 공개돼도
# 되는가"에 먼저 답한다.
VITE_API_BASE_URL=http://localhost:8000
